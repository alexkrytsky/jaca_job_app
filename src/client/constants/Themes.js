import { createMuiTheme } from '@material-ui/core';

const Themes = [
  // MSC General Light
  createMuiTheme({
    palette: {
      type: 'light',
      primary: { main: '#9CAF88' },
      secondary: { main: '#009681' },
    }
  }),
];

export default Themes;
